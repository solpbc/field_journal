.env
.venv
*.egg-info/
__pycache__/
*.pyc
*.pyo
.coverage
.pytest_cache/
.mypy_cache/
.installed
.DS_Store
.agents/
.claude/
scratch/
tools/.cache/

# Journal config holds API keys, convey secret, and identity — never commit.
# A `make config` target re-materializes a minimal onboarded config on demand.
journal/config/
